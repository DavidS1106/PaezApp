import React from 'react';

// this is the equivalent to the createStore method of Redux
// https://redux.js.org/api/createstore

const IsConnectedContext = React.createContext(false);

export default IsConnectedContext;